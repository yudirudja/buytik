<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.monagealpha">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Monage"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AkunActivity"></activity>
        <activity android:name=".PengaturanActivity" />
        <activity
            android:name=".NavigationDrawer"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LupaPassword" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".login" />
        <activity android:name=".KategoriPengluaranActivity" />
        <activity
            android:name=".splash"
            android:label="Monage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KategoriPemasukanActivity" />
        <activity android:name=".InputActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>